@model IEnumerable<AdminLTE.MVC.Models.tblState>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblCountry)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tblCountry.country_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.state_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.state_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.state_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
