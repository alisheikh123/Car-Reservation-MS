@model IEnumerable<AdminLTE.MVC.Models.tblCity>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblState)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tblState.state_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.city_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.city_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.city_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
